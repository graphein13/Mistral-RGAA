import { ServerArgs } from './index';
import { ServerReadyData } from './api';
import { BrowserFeature } from '../constants';
import { ToolRunnerTree } from './tool-runner';
interface CustomGuiError {
    response: {
        status: number;
        data: string;
    };
}
export type GetInitResponse = (ToolRunnerTree & {
    customGuiError?: CustomGuiError;
} & {
    browserFeatures: Record<string, BrowserFeature[]>;
}) | null;
export declare const start: (args: ServerArgs) => Promise<ServerReadyData>;
export {};
