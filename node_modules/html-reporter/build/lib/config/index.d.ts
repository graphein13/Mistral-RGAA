import { ReporterConfig, ReporterOptions } from '../types';
type AssertionFn<T> = (value: unknown) => asserts value is T;
export declare const assertNumber: (name: string) => AssertionFn<number>;
export declare const parseConfig: (options: Partial<ReporterOptions>) => ReporterConfig;
export {};
