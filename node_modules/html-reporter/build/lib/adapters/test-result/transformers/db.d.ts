import { ReporterTestResult } from '../index';
import { DbTestResult } from '../../../sqlite-client';
interface Options {
    baseHost?: string;
}
export declare class DbTestResultTransformer {
    private _options;
    constructor(options: Options);
    transform(testResult: ReporterTestResult): DbTestResult;
}
export {};
